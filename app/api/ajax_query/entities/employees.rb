# frozen_string_literal: true

module AjaxQuery
  module Entities
    class Employees < Rails::ApplicationController
      # %i[unique_id preferred_name department].each do |property|

      # %i[first_name last_name email netid].each do |property|
    end
  end
end
