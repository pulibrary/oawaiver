<%= form_for(@account) do |f| %>
    <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group field">
      <%= f.label :netid, 'Netid' %>
      <%= f.text_field :netid, class: 'form-control' %>
    </div>

  <div class="actions">
    <%= f.submit 'Create Account', class: 'btn btn-orange' %>
  </div>
<% end %>
